import type { TripSection } from '../../../lib/trips/types';

export const tripSectionTitles: Record<TripSection, string> = {
  attractions: 'Ride picks',
  schedule: 'Day-by-day plan',
  party: 'Traveler preferences',
};
